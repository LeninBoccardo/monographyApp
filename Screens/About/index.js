import React from 'react';
import { 
    View, 
    Text, 
    ScrollView, 
    SafeAreaView,
    Image,
} from 'react-native';

// TODO all

export default function About({ navigation, route }) {
    return (
        <SafeAreaView>
            <ScrollView>
                <View>
                    <Text>
                        
                    </Text>
                </View>
                
            </ScrollView>
        </SafeAreaView>
    );
};